create table Device(
    id int generated by default as identity primary key,
    name varchar(100),
    created_at timestamp,
    created_why varchar(100)
);

create table Measurements(
    id int generated by default as identity primary key,
    value decimal,
    raining boolean,
    device_id int references Device(id),
    created_at timestamp
);

